# Real-model validation v0.6 pre-code audit

Clean baseline 95c2f89; development-only origin, upstream push disabled, executable production-blocking hook. Read actual v0.5 output/Proposal schemas, provider/prompt/hash, all 62 Analyst tests, validator/identity/store/promotion, Gold32/context19/literal16, v0.3 Chunk/citation semantics, v0.4 human Admission and v0.5 delivery. Existing formulas/UI/API/Memory/Admission/Retrieval/Gold/prompt/validator stay unchanged.

Gold replay16 findings overlap: ambiguous_period_binding16; ambiguous_value_binding13; numeric_unit_binding_invalid8; negated_numeric_not_admissible9; guidance_misclassified7; unknown_or_unsupported_unit5; period_not_supported5. No invalid quote, wrong chunk/locator, schema/temporal/normalization failures. Oracle fields match Gold but every quote is an entire Chunk. Table headers/units are separated from row cells and unrelated years/negations/expected-recognition text contaminate global heuristics. This is a fixture/context representation limitation and exposes potential false-rejection heuristics, not evidence that citation checks should be removed. Correctness of each actual model output will be compared independently before assigning false rejection. No validator/prompt tuning on locked Gold.

Existing external DeepSeek credential exists as a JSON-quoted scalar in the established .credentials.yaml, permission600. Never print/save it; read only in memory. No provider env credential or local model executable was found in scoped discovery. Use pure direct HTTPS structured analysis, no harness/tools/risk context and no new dependency. Real calls explicit opt-in; deterministic CI never networks. DeepSeek official Responses API documentation supports native JSON Schema, usage and stateless responses; temperature0 with reasoning disabled, model snapshot unknown until reported. API failure is recorded honestly, never replaced with mock metrics.

Benchmark will use locked Chunk references, offsets/hash/manual binding notes and unchanged Gold IDs/values; bypass Retrieval entirely for model input. Separate model output, v1 validator result and independent locked support/Gold comparison; uncertain support remains UNKNOWN, not automatically false rejection. Raw responses/Proposal inputs/runtime stay outside repository, committed results retain safe fields/hashes/metrics. No Admission in real evaluation; separate existing compatibility tests preserve human-only writes.
