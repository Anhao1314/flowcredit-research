# Contract stabilization v0.7 pre-code diagnosis

Baseline b3b3e5c clean; origin development repository, production push disabled, hook executable, isolation passed. Read v0.5 prompt/schema/provider/validator/promotion, actual DeepSeek adapter, v0.6 harness, every locked16 case, all19 first-pass structural failures and22 contract findings, original external raw failure prefixes, confirmed date case and token/latency metadata, and v0.6 documentation.

1. Majority fields: statement equality21, unsupported/noncanonical unit21, unknown period19, category unsupported14, category-unit mismatch10. Two unsupported normalizations. Findings overlap, not case counts.
2. Formatting:14 Gold responses fenced JSON;5 numeric confusing responses echoed schema or root object.19 first-pass failures, plus2 failed repeats. Native schema request did not ensure actual structure.
3. Semantic: unsuitable detailed categories, risk statements paraphrased; field agreement must be separated from source-number support. Raw20 numeric values were physically supported. Table headers are separated from cells; cannot manufacture prose.
4. Normalization: model emitted natural unit labels and identity amounts in millions; canonical scale arithmetic belongs to deterministic code.
5. Date: unknown observation dates; June2024 became2024-06-30 without day support. Exact observation day cannot be invented. FY/quarter parsing must disclose calendar interpretation; final unchanged validator can still reject abbreviated dates.
6. Move trusted subject/source/chunk identity, provenance/hash/time, statement=quote, canonical units, exact decimal scale arithmetic and conservative calendar parsing to code. LLM discovers narrow source spans and interprets raw literal substrings only. No Memory/Admission authority, no API/UI/risk/Retrieval changes. Source identity/citation/temporal freshness is checked before both passes and promotion.

Contract v1/raw results remain immutable. New independent development fixtures precede one final locked16 evaluation. No provider switch; DeepSeek Responses path and requested model/temperature remain primary comparison. Native schema is requested; one optional strictly delimited outer JSON fence can be removed with raw retained/hash recorded. Never repair fields, prose, roots or semantic values. Existing validator remains unchanged.
