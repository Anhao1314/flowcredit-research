# OS junk
.DS_Store
Thumbs.db

# local scratch
*.local

# deployment secrets and private credentials
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
credentials*
secrets*

# local research runtime artifacts (pipelines keep these outside the repo by default)
*.sqlite
*.sqlite3
*.db
*.log

# one local machine-state trace; the spike's reproducibility artifacts stay tracked
research/claim-reasoning-spike/artifacts/cleanup.json
